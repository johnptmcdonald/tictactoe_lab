// WIN LOGIC
//

self.Board[index].occupied = currentIcon

// Horizontal from left to right and top to bottom for X
if self.Board[0].occupied = 'X' && self.Board[4].occupied = 'X' && self.Board[7].occupied = 'X'
if self.Board[1].occupied = 'X' && self.Board[5].occupied = 'X' && self.Board[8].occupied = 'X'
if self.Board[2].occupied = 'X' && self.Board[6].occupied = 'X' && self.Board[9].occupied = 'X'


function getWinner(
if self.Board[0].occupied = 'X' && self.Board[4].occupied = 'X' && self.Board[7].occupied = 'X'
if self.Board[1].occupied = 'X' && self.Board[5].occupied = 'X' && self.Board[8].occupied = 'X'
if self.Board[2].occupied = 'X' && self.Board[6].occupied = 'X' && self.Board[9].occupied = 'X') {
	console.log(X WINS!!!!!)
}

// VARIABLES

var playerX = 'playerX'; // turn = 0
var playerO = 'playerO'; // turn = 1
var numberOfMoves = 0
var currentPlayer = ''
// var turn = 0; // toggles btw 0 and 1 for switching turns
// var singleMove

// var board = [
// 		[0, 0, 0],
// 		[0, 0, 0],
// 		[0, 0, 0]
// ];

var cell1 = null
var cell2 = null
var cell3 = null
var cell4 = null
var cell5 = null
var cell6 = null
var cell7 = null
var cell8 = null
var cell9 = null


function cell1Move() {

	if (currentPlayer == 'playerX' && cell1 == null){
		cell1 = 'playerX'
		numberOfMoves +=
	} else if (currentPlayer == 'playerO' && cell1 == null) {
		cell1 = 'playerO'
		numberOfMoves +=
	} else {
		alert('Please choose an available cell')
	};

function cell2Move() {

	if (currentPlayer == 'playerX' && cell2 == null){
		cell2 = 'playerX'
	} else if (currentPlayer == 'playerX' && cell2 == null) {
		cell2 = 'playerO'
	} else {
		alert('Please choose an available cell')
	};




if numberOfMoves%2 = 0 { currentPlayer = playerOturn }

playerX
playerO

function numberOfMoves
for each gameMove, numberOfMoves +=1

function singleMove(){

	if (currentPlayer == 'playerX' && cell1 == null){
		cell1 = 'playerX'
	} else if (currentPlayer == 'playerX' && cell1 == null) {
		cell1 = 'playerO'
	} else {
		alert('Please choose an available cell')
	};


	document.getElementById('cell1') = "X";
	numberOfMoves+=

}

if (numberOfMoves%2 = 0){
	currentTurn = playerTwo
}
else
	currentTurn = PlayerOne
}



function getWinner(){
	document.getElementById('cell1').value == "X" &&
	document.getElementById('cell1').value == "X"
}{alert("THE WINNER IS X!")}


if playerAResponse == result

Player ...random... choose heads or tails
Randomizer... heads/tails
If playerinput == randomanswer, player select "gofirst" or "gosecond"

function Moves(){

}
for (var i=0; i < self.cells.length; i++) {

}

if (var i%2=0) {
	playerBturn = true;
}
else {
	playerAturn = true;
}

function firstMove (){
	eventListener
}

switch to playerTwo

secondMove


"Input A" = PlayerOne
- FirstMove, third

"Input B" = PlayerTwo
- Second Move
If 2% = 0

Player one place marker

getElementByClass("cell")
- add eventListener / or ng-click

If void, change to occupied

Player two place marker

Declare cats game


Declare "player#" as winner

Clear board : function

moveCount

Start game

Random heads or tails to determine starting player
Player clicks to select (heads or tails):
- If "selection" == "result", player receives option to go first or second

onClick change appearance and occupancy of box
- boxOccupied
- boxVacant
- eventListener (?)

Reset scores as a button (?)

addl : on Hover change appearance of box

addl : Player One choose icon / Player Two choose icon
note: X = blue, O = red

Ways to win:
// Horizontal wins from top to bottom
cell 1 == cell 2 == cell 3
cell 4 == cell 5 == cell 6
cell 7 == cell 8 == cell 9

// Vertical wins from left to right
cell 1 == cell 4 == cell 7
cell 2 == cell 5 == cell 8
cell 3 == cell 6 == cell 9

// Diagonal wins
cell 1 == cell 5 == cell 9
cell 3 == cell 5 == cell 7


// Create a claimCell function : This one works
self.click = function(index) {
	self.Board[index].occupied = currentIcon;
	numberOfMoves = numberOfMoves + 1
	changePlayers()
	iconDefine()
	console.log('xxx')
}


function getWinner() {
	if (self.Board[0].occupied = 'X' &&
		self.Board[1].occupied = 'X' &&
		self.Board[2].occupied = 'X')
	{
		console.log(X WINS!)
	}
};

// Detect a win ; and secondarily - Have player's nickname equal each cell's occupancy

// var person = prompt("Please enter your name", "Harry Potter");
// var playerA = "+s+"
// var playerA = prompt("Please enter your name", "Your name here");
// var playerA, playerB
//
// while ( ! playerA ) playerA = prompt ("Please enter your name #1" );
// while ( ! playerB ) playerB = prompt ("Please enter your name #2" );

// alert( "Alright" + playerA + playerB ". Let's play!" );

// Game Initiates
// Board is clear

// {{ self.cells }}

var Sq1 = null;
var Sq2 = null;
var Sq3 = null;
var Sq4 = null;
var Sq5 = null;
var Sq6 = null;
var Sq7 = null;
var Sq8 = null;
var Sq9 = null;

var board = [
	[Sq1, Sq2, Sq3],
	[Sq4, Sq5, Sq6],
	[Sq7, Sq8, Sq9],
	];

// Determine game order
// Note: Organize an entire game for one player then add

// getElementsByClass('cell') > onclick change occupied to {{ player.name }}

// Player A Occupies a cell

function changeSquare(){
	occupied = '{{ players.Name }}'
};

// Switches to Player B
// Player B Occupies a cell
...
// Check board after 5 turns
// Check board after each turn

// Check board for win
// ^ Use a for loop